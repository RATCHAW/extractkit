{
  "name": "extractkit-monorepo",
  "private": true,
  "packageManager": "pnpm@10.17.0",
  "engines": {
    "node": ">=20.19"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "run-eval": "turbo run run-eval",
    "report": "turbo run report",
    "fetch-data": "turbo run fetch-data",
    "pin": "turbo run pin"
  },
  "devDependencies": {
    "turbo": "^2.10.4"
  }
}
